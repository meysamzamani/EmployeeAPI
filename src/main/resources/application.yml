spring:
  application:
    name: employee_api
  datasource:
    url: jdbc:postgresql://localhost:5432/meysamzamani
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
server:
  port: 8080
  compression:
    enabled: true
  error:
    include-message: always